uploads/products/*
!uploads/products/.gitkeep
uploads/invoices/*
!uploads/invoices/.gitkeep
!uploads/invoices/.htaccess
uploads/legal_documents/*
!uploads/legal_documents/.gitkeep
uploads/rma/*
!uploads/rma/.gitkeep
config/config.local.php
config/installed.php
*.log
.DS_Store
Thumbs.db

# IDE / editor
.idea/
.vscode/
*.swp

# Claude Code project settings (local-only permissions, not project config)
.claude/settings.local.json

# Composer (only if you opt into PHPMailer/dompdf/etc. per the README)
vendor/
composer.lock

# Private planning doc - not for the repo
claude_prompt.md
